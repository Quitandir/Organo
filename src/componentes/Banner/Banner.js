import './Banner.css';

function Banner () {
    // JSX: é como o react trabalha com a parte visual. Parece HTML, mas não é.
    return (
        <header className='banner'>
            <img src="/img/banner.png" alt="Imagem principal do banner da Organo"></img>

        </header>
    )
}

export default Banner